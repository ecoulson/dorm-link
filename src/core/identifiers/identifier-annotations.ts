import { Annotation } from 'noose-injection';

export const UUIDGenerationFunctionAnnotation = new Annotation(
    'UUIDGenerationFunction'
);
export const UUIDGeneratorAnnotation = new Annotation('UUIDGenerator');
